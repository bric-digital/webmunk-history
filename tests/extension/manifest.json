{
  "manifest_version": 3,
  "name": "Webmunk History Test Extension",
  "version": "0.0.1",
  "description": "Minimal Chrome extension used by Playwright extension-mode tests. Loads only the webmunk-history service worker module.",
  "permissions": ["alarms", "history", "storage"]
  ,
  "background": {
    "service_worker": "service-worker.bundle.js",
    "type": "module"
  }
}
